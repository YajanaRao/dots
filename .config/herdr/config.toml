[keys]
# alt is reserved for AeroSpace, so navigation avoids it
next_agent = "prefix+]"
previous_agent = "prefix+["
focus_agent = "prefix+ctrl+1..9"
switch_workspace = "prefix+shift+1..9"

[terminal]
default_shell = "/opt/homebrew/bin/nu"

[theme]
name = "terminal"
auto_switch = false
